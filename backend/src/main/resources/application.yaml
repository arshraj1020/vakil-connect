spring:
  application:
    name: vakilconnect

  datasource:
    url: jdbc:postgresql://localhost:5432/vakilconnect
    username: arshraj
    password:

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: update