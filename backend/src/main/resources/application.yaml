spring:
  application:
    name: vakilconnect

  datasource:
    url: jdbc:postgresql://localhost:5432/vakilconnect
    username: arshraj
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080